#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define e cout << endl

using namespace std;

int main ()
{
	int t ; cin >> t;
	while(t--) {
		int n , x; cin >> n ;
		int a[10]= {};
 
		for(int i = 0 ; i < n ; i++) {
			cin >> x ;
			while(x) {
				int m = x % 10 ;
				a[m]++;
				x /= 10;  
			}
		}
		for(int i = 0 ; i < 10 ; i++) {
			if(a[i]) cout << i << " "; 
		} 
		e; 
	}
}

	

