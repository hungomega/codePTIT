#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define e cout << endl

using namespace std;

int main ()
{
	int query ; cin >> query;
	string s ;
	queue<int> q ;
	while(query--) {
		cin >> s ;
		if(s == "PUSH") {
			int x ;cin >> x;
			q.push(x);
		}
		else if(s == "POP") {
			if(!q.empty()) q.pop();
		}
		else if(s == "PRINTFRONT") {
			if(q.empty()) cout << "NONE\n";
			else cout << q.front() << endl ;
		}
	} 
}
