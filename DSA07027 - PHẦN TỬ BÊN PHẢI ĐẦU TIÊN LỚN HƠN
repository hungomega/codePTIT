#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define e cout << endl
#define pb push_back 

int main ()
{
	int t ; cin >> t;
	while(t--) 
	{
		int n ; cin >> n ; int a[n];
		//stack<int> st ;
		for(int i = 0 ; i < n ; i++) {
			cin >> a[i];
		}
		for(int i = 0 ; i < n ; i++) {
			int check = 1 ;
			for(int j = i + 1 ; j < n ; j++) {
				if(a[j] > a[i]) {
					check = 0 ;
					cout << a[j] << " ";
					break ;
				}
			}
			if(check) cout << "-1" << " ";
		} 
		e;
	}
}













			
    









